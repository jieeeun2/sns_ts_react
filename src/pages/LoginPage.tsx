import styled from 'styled-components'

const LoginPage = () => {
  return <LoginPageLayout>LoginPage</LoginPageLayout>
}

export default LoginPage

const LoginPageLayout = styled.div`
  height: calc(100dvh - 90px);
`
